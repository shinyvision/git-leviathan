# leviathan.repository

Auto-generated by `cargo xtask gen-docs`. Do not edit by hand.

Active repository snapshot plus typed read APIs.

- Version: `1.0` (v1)
- Stability: `stable`
- Migration promise: source-compatible additions only within host 1.x
- Capabilities: `git:read:status`, `git:read:log`, `git:read:diff`, `git:read:show`, `git:read:blame`

## Types

### `leviathan.repository`

Active repository snapshot.

- `name` (`string`; required) - Repository display name.
- `workdir_path` (`string`; required) - Workdir path, empty when unavailable.
- `current_branch_name` (`string`; required) - Current branch display name.
- `current_branch` (`LeviathanLocalBranch|nil`; required) - Current local branch table.
- `is_open` (`boolean`; required) - Whether a repository is loaded.
- `is_detached` (`boolean`; required) - Whether HEAD is detached.
- `is_unborn` (`boolean`; required) - Whether HEAD points at an unborn branch.
- `is_bare` (`boolean`; required) - Whether the repository has no worktree.
- `head_hash` (`string`; required) - Current HEAD hash or empty string.
- `default_remote_name` (`string`; required) - Default remote name or empty string.
- `local_branches` (`LeviathanLocalBranch[]`; required) - Local branches.
- `remote_branches` (`LeviathanRemoteBranch[]`; required) - Remote branches.
- `tags` (`LeviathanTag[]`; required) - Tags.

### `LeviathanLocalBranch`

Local branch.

- `name` (`string`; required) - Branch name.
- `hash` (`string`; required) - Target hash.
- `is_current` (`boolean`; required) - Whether this branch is current.
- `upstream_branch` (`LeviathanRemoteBranch|nil`; required) - Upstream remote branch.

### `LeviathanRemoteBranch`

Remote branch.

- `name` (`string`; required) - Branch name.
- `remote_name` (`string`; required) - Remote name.
- `hash` (`string`; required) - Target hash.

### `LeviathanTag`

Git tag.

- `name` (`string`; required) - Tag name.
- `hash` (`string`; required) - Target hash.

### `LeviathanCommit`

Commit data exposed to Lua APIs.

- `kind` (`string`; required) - Commit row kind: commit, dirty, or stash.
- `hash` (`string`; required) - Full commit hash, or empty for an uncommitted dirty row.
- `short_hash` (`string`; required) - Short display hash.
- `summary` (`string`; required) - First line of the commit message.
- `message` (`string`; required) - Full commit message when available.
- `author` (`string`; required) - Commit author display name.
- `date` (`string`; required) - Host-formatted commit date when available.
- `timestamp` (`integer|nil`; required) - Author timestamp in seconds when available.
- `parents` (`string[]`; required) - Full parent hashes in git parent order.
- `index` (`integer|nil`; required) - Zero-based visible commit row index when available.
- `is_merge` (`boolean`; required) - Whether the commit has multiple parents.
- `is_merge_in_progress` (`boolean`; required) - Whether the dirty row represents an in-progress merge.
- `actions` (`LeviathanCommitActions`; required) - Host-computed commit actions.

### `LeviathanCommitActions`

Host-computed actions for a commit.

- `reword` (`LeviathanActionAvailability`; required) - Reword availability for this commit in the current tree.

### `LeviathanActionAvailability`

Availability state for a host action.

- `enabled` (`boolean`; required) - Whether the action can run.
- `reason` (`string|nil`; required) - Stable disabled reason when available.

### `LeviathanCommitDiff`

Diff loaded for a commit.

- `commit` (`LeviathanCommit`; required) - Commit this diff was loaded for.
- `modified_count` (`integer`; required) - Modified file count.
- `added_count` (`integer`; required) - Added file count.
- `deleted_count` (`integer`; required) - Deleted file count.
- `files` (`LeviathanCommitDiffFile[]`; required) - Changed files in the commit diff.

### `LeviathanCommitDiffFile`

File row in a commit diff.

- `path` (`string`; required) - Repository-relative file path.
- `status` (`string`; required) - Change status.

### `LeviathanCommitFile`

File snapshot loaded at a commit.

- `commit` (`LeviathanCommit`; required) - Commit this file snapshot was loaded for.
- `path` (`string`; required) - Repository-relative file path.
- `lines` (`string[]`; required) - Per-line diff content.
- `old_content` (`string`; required) - Old file content when available.
- `new_content` (`string`; required) - New file content when available.

## Events

- `BranchChanged` (table) - Fired after the current branch or its head hash changes.
  - `name` (`string`; required) - Branch name.
  - `head_hash` (`string`; required) - Resolved HEAD commit hash.
- `RefsChanged` (table) - Fired after the repository ref set changes (push/fetch/branch ops).
  - `count` (`integer`; required) - Number of refs after the change.
- `HeadChanged` (table) - Fired after the resolved HEAD commit hash changes.
  - `hash` (`string`; required) - New HEAD commit hash.
- `CommitListChanged` (table) - Fired after the visible commit list changes.
  - `count` (`integer`; required) - Number of commits in the new list.

## Functions

### `leviathan.repository.current()`

Return the cached `leviathan.repository` snapshot table for the active repo.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `table` - Active repository snapshot.

### `leviathan.repository.refs()`

Return the active repository's typed refs (locals, remotes, tags). Returns (refs, nil) or (nil, err).

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `table|nil` - Refs snapshot on success.
- `string|nil` - Error message on failure.

### `leviathan.repository.head()`

Return the active repository's HEAD info: { hash, ref, detached }.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `table|nil` - HEAD snapshot on success.
- `string|nil` - Error message on failure.

### `leviathan.repository.status()`

Return the working-tree status: { staged[], unstaged[], conflicted[] }.

**Capabilities:** `git:read:status`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `table|nil` - Status snapshot on success.
- `string|nil` - Error message on failure.

### `leviathan.repository.commits(opts)`

Return up to `limit` commits from the active repository as `LeviathanCommit` rows.

**Capabilities:** `git:read:log`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `opts` (`table`; optional) - Options table: { limit = 100, rev = "HEAD" }.

**Returns:**
- `LeviathanCommit[]|nil` - Commit list on success.
- `string|nil` - Error message on failure.

### `leviathan.repository.diff(opts)`

Return per-file diff for `commit`. The result carries `commit` as `LeviathanCommit`.

**Capabilities:** `git:read:diff`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `opts` (`table`; required) - Options table: { commit = LeviathanCommit }.

**Returns:**
- `LeviathanCommitDiff|nil` - Diff snapshot on success.
- `string|nil` - Error message on failure.

### `leviathan.repository.file_at(opts)`

Return the file at a given commit/path. The result carries `commit` as `LeviathanCommit`.

**Capabilities:** `git:read:show`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `opts` (`table`; required) - Options table: { commit = LeviathanCommit, path = "src/main.rs" }.

**Returns:**
- `LeviathanCommitFile|nil` - File-at-commit snapshot on success.
- `string|nil` - Error message on failure.

### `leviathan.repository.blame(opts)`

Blame is not yet wired through the host gateway. Returns (nil, "unsupported").

**Capabilities:** `git:read:blame`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `opts` (`table`; required) - Options table: { path = "src/main.rs", line_range = {start,end} }.

**Returns:**
- `nil` - Always nil.
- `string` - Error message.

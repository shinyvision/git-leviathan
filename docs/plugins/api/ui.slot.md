# leviathan.ui.slot

Auto-generated by `cargo xtask gen-docs`. Do not edit by hand.

UI slot namespace.

- Version: `1.0` (v1)
- Stability: `stable`
- Migration promise: source-compatible additions only within host 1.x
- Capabilities: `ui:region:<region>`

## Types

### `leviathan.ui.slot`

UI slot namespace.

### `LeviathanSlotSpec`

UI slot.

- `region` (`string`; required) - Region name.
- `pane` (`string`; optional) - Content region pane.
- `section` (`string`; required) - Region section.
- `id` (`string`; required) - Slot id.
- `priority` (`integer`; required) - Ordering priority.
- `widget` (`LeviathanWidget|fun(ctx: LeviathanUiContext): LeviathanWidget`; required) - Static widget or dynamic widget with context.
- `depends_on` (`string[]`; optional) - Dynamic refresh dependencies: plugin_state, repository, tab, selection, diff, theme, layout.
- `on_click` (`fun(slot_id: string, event: string, value: LeviathanJson): table|nil`; optional) - Slot callback.

### `LeviathanSlotTarget`

UI slot target.

- `plugin_id` (`string`; optional) - Target owner. Omit for the current plugin or builtin slots.
- `region` (`string`; required) - Region name.
- `pane` (`string`; optional) - Content region pane.
- `section` (`string`; required) - Region section.
- `id` (`string`; required) - Slot id.

### `LeviathanSlotHandle`

Ledger-backed UI slot handle.

- `plugin_id` (`string`; required) - Target owner.
- `region` (`string`; required) - Region name.
- `pane` (`string`; optional) - Content region pane.
- `section` (`string`; required) - Region section.
- `id` (`string`; required) - Slot id.
- `address` (`LeviathanSlotTarget`; required) - Full slot address.

- `LeviathanSlotHandle:remove()` - Remove this slot if the handle is still live.
- `LeviathanSlotHandle:replace(spec)` - Replace this slot if the handle is still live.

## Functions

### `leviathan.ui.slot.add(spec)`

Add a slot and return a ledger-backed handle.

**Capabilities:** `ui:region:<region>`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `spec` (`LeviathanSlotSpec`; required) - Slot spec including region, section/pane, id, priority, and widget.

**Returns:**
- `LeviathanSlotHandle|nil` - Slot handle on success, nil on failure.
- `string|nil` - Error message on failure.

### `leviathan.ui.slot.remove(target)`

Remove a slot by full address.

**Capabilities:** `ui:region:<region>`, `ui:remove:builtin`, `ui:remove:<region>:<container>:<id>`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `target` (`LeviathanSlotTarget`; required) - Slot address including region, section/pane, and id.

**Returns:**
- `boolean|nil` - True on success, nil on failure.
- `string|nil` - Error message on failure.

### `leviathan.ui.slot.replace(target, spec)`

Replace a slot by full address and return a handle.

**Capabilities:** `ui:region:<region>`, `ui:replace:builtin`, `ui:replace:<region>:<container>:<id>`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `target` (`LeviathanSlotTarget`; required) - Existing slot address including region and id.
- `spec` (`LeviathanSlotSpec`; required) - Replacement slot spec.

**Returns:**
- `LeviathanSlotHandle|nil` - Slot handle on success, nil on failure.
- `string|nil` - Error message on failure.

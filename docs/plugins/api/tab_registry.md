# leviathan.tab_registry

Auto-generated by `cargo xtask gen-docs`. Do not edit by hand.

Read-only tab snapshot plus queued tab mutation APIs.

- Version: `1.0` (v1)

## Types

### `leviathan.tab_registry`

Tab registry namespace.

- `list` (`LeviathanTab[]`; required) - Open tabs.
- `current` (`LeviathanTab|nil`; required) - Currently focused tab.

### `LeviathanTab`

Open tab descriptor.

- `path` (`string`; required) - Tab repository path.
- `name` (`string`; required) - Tab display name.

## Events

- `TabAdded` (table) - Fired after a tab is added.
  - `tab_id` (`integer`; required) - Tab id.
  - `path` (`string`; optional) - Repository path the tab is bound to, when known.
- `TabRemoved` (table) - Fired after a tab is removed.
  - `tab_id` (`integer`; required) - Tab id.
  - `path` (`string`; optional) - Repository path the tab is bound to, when known.
- `TabMoved` (table) - Fired after tabs are reordered.
  - `count` (`integer`; required) - Number of open tabs after the move.
- `TabSelected` (table) - Fired after the active tab changes.
  - `tab_id` (`integer`; required) - Tab id.
  - `path` (`string`; optional) - Repository path the tab is bound to, when known.

## Functions

### `leviathan.tab_registry.add(path)`

Open or focus a tab for the given path.

**Parameters:**
- `path` (`string`; required) - Path string.

### `leviathan.tab_registry.remove(path)`

Close the tab at path.

**Parameters:**
- `path` (`string`; required) - Path string.

### `leviathan.tab_registry.select(path)`

Focus the tab at path.

**Parameters:**
- `path` (`string`; required) - Path string.

### `leviathan.tab_registry.reorder(paths)`

Reorder tabs to match the given path list.

**Parameters:**
- `paths` (`string[]`; required) - Tab paths in target order.


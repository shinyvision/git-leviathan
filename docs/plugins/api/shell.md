# leviathan.shell

Auto-generated by `cargo xtask gen-docs`. Do not edit by hand.

Host default shell namespace.

- Version: `1.0` (v1)
- Stability: `stable`
- Migration promise: source-compatible additions only within host 1.x
- Capabilities: `process:spawn`

## Types

### `leviathan.shell`

Host default shell namespace.

- `name` (`string`; required) - Resolved shell display name.
- `is_available` (`boolean`; required) - Whether the shell executable is available on this host.

### `LeviathanShellRunSpec`

Shell command execution spec.

- `command` (`string`; required) - Command string passed to the shell.
- `cwd` (`string|nil`; optional) - Working directory.
- `env` (`table<string,string>|nil`; optional) - Environment variables to add or override.
- `input` (`string|nil`; optional) - Stdin payload.
- `timeout_ms` (`integer|nil`; optional) - Optional timeout in milliseconds.
- `max_output_bytes` (`integer|nil`; optional) - Per-stream output capture limit.
- `ansi` (`boolean|nil`; optional) - Set color-friendly terminal environment variables.

### `LeviathanShellOpenSpec`

Interactive PTY shell session spec.

- `cwd` (`string|nil`; optional) - Working directory for the interactive shell.
- `env` (`table<string,string>|nil`; optional) - Environment variables to add or override.
- `rows` (`integer|nil`; optional) - Initial terminal row count.
- `cols` (`integer|nil`; optional) - Initial terminal column count.

### `LeviathanShellRunResult`

Completed shell command result.

- `shell` (`string`; required) - Shell name used for execution.
- `command` (`string`; required) - Command string.
- `cwd` (`string`; required) - Working directory or empty string.
- `success` (`boolean`; required) - Whether the process exited successfully.
- `status` (`string`; required) - success, failed, or timed_out.
- `exit_code` (`integer|nil`; required) - Process exit code when available.
- `stdout` (`string`; required) - Captured stdout.
- `stderr` (`string`; required) - Captured stderr.
- `combined` (`string`; required) - stdout and stderr concatenated for simple terminal rendering.
- `timed_out` (`boolean`; required) - Whether timeout killed the process.

### `LeviathanShellJobHandle`

Cancellable shell job handle.

- `LeviathanShellJobHandle:cancel()` - Request cancellation and kill the running process.
- `LeviathanShellJobHandle:id()` - Return host job id.

## Functions

### `leviathan.shell.run(spec, on_complete)`

Run a command through the host default shell on a worker thread.

**Capabilities:** `process:spawn`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `spec` (`string|LeviathanShellRunSpec`; required) - Command string or shell run spec.
- `on_complete` (`fun(ok:boolean, result:LeviathanShellRunResult|string)|nil`; optional) - Completion callback run on the host Lua thread.

**Returns:**
- `LeviathanShellJobHandle|nil` - Cancellable job handle on success.
- `string|nil` - Error message when the command could not be started.

### `leviathan.shell.open(spec)`

Open an interactive PTY session using the host default shell.

**Capabilities:** `process:spawn`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `spec` (`LeviathanShellOpenSpec|nil`; optional) - Optional PTY session settings.

**Returns:**
- `integer|nil` - Terminal session id on success.
- `string|nil` - Error message when the session could not be opened.

### `leviathan.shell.write(session, data)`

Write text or control bytes to a PTY session owned by the plugin.

**Capabilities:** `process:spawn`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `session` (`integer`; required) - Terminal session id.
- `data` (`string`; required) - Text or control bytes to write to the PTY.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.shell.resize(session, cols, rows)`

Resize a PTY session owned by the plugin.

**Capabilities:** `process:spawn`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `session` (`integer`; required) - Terminal session id.
- `cols` (`integer`; required) - Terminal column count.
- `rows` (`integer`; required) - Terminal row count.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.shell.close(session)`

Close a PTY session owned by the plugin.

**Capabilities:** `process:spawn`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `session` (`integer`; required) - Terminal session id.

**Returns:**
- `boolean` - Boolean result.

### `leviathan.shell.is_running(session)`

Return whether a PTY session owned by the plugin is still running.

**Capabilities:** `process:spawn`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `session` (`integer`; required) - Terminal session id.

**Returns:**
- `boolean` - Boolean result.

# leviathan.fs

Auto-generated by `cargo xtask gen-docs`. Do not edit by hand.

Filesystem and path helper APIs.

- Version: `1.0` (v1)
- Stability: `stable`
- Migration promise: source-compatible additions only within host 1.x
- Capabilities: `fs:read`, `fs:write:*`

## Types

### `leviathan.fs`

Filesystem namespace.

### `LeviathanFsEntry`

Filesystem entry metadata.

- `name` (`string`; required) - Entry name.
- `path` (`string`; required) - Full path.
- `is_dir` (`boolean`; required) - Whether the entry is a directory.
- `is_symlink` (`boolean`; required) - Whether the entry is a symlink.
- `size` (`integer`; required) - Entry size.
- `modified` (`integer`; required) - Unix modification time.

## Functions

### `leviathan.fs.read_file(path)`

Read UTF-8 file contents. Returns content or (nil, err).

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `string|nil` - Value on success, nil on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.read_lines(path)`

Read a UTF-8 file as an array of lines.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `string[]|nil` - Lines on success.
- `string|nil` - Error message on failure.

### `leviathan.fs.write_file(path, content)`

Write content to a file, replacing existing contents.

**Capabilities:** `fs:write:*`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Target path.
- `content` (`string`; required) - UTF-8 content.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.append_file(path, content)`

Append content to a file, creating it when absent.

**Capabilities:** `fs:write:*`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Target path.
- `content` (`string`; required) - UTF-8 content.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.delete(path)`

Delete a file or directory tree.

**Capabilities:** `fs:write:*`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.mkdir(path)`

Create a directory and missing parents.

**Capabilities:** `fs:write:*`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.copy(src, dst)`

Copy a regular file.

**Capabilities:** `fs:read`, `fs:write:*`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `src` (`string`; required) - Source path.
- `dst` (`string`; required) - Destination path.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.rename(src, dst)`

Move or rename a path.

**Capabilities:** `fs:write:*`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `src` (`string`; required) - Source path.
- `dst` (`string`; required) - Destination path.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.touch(path)`

Create a file or update its modification time.

**Capabilities:** `fs:write:*`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.read_link(path)`

Read a symlink target without following it.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `string|nil` - Value on success, nil on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.list_dir(path)`

List directory entries sorted with directories first.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `LeviathanFsEntry[]|nil` - Directory entries on success.
- `string|nil` - Error message on failure.

### `leviathan.fs.exists(path)`

Return whether a path exists.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `boolean` - Boolean result.

### `leviathan.fs.is_file(path)`

Return whether a path is a regular file.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `boolean` - Boolean result.

### `leviathan.fs.is_dir(path)`

Return whether a path is a directory.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `boolean` - Boolean result.

### `leviathan.fs.is_symlink(path)`

Return whether a path itself is a symlink.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `boolean` - Boolean result.

### `leviathan.fs.size(path)`

Return symlink metadata size in bytes.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `integer|nil` - Byte size on success.
- `string|nil` - Error message on failure.

### `leviathan.fs.modified(path)`

Return modification time as Unix seconds.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `number|nil` - Unix timestamp on success.
- `string|nil` - Error message on failure.

### `leviathan.fs.metadata(path)`

Return metadata shaped like a single list_dir entry.

**Capabilities:** `fs:read`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `LeviathanFsEntry|nil` - Entry metadata on success.
- `string|nil` - Error message on failure.

### `leviathan.fs.is_absolute(path)`

Return whether a path string is absolute.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `boolean` - Boolean result.

### `leviathan.fs.parent(path)`

Return the parent path string, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `string|nil` - Parent path.

### `leviathan.fs.basename(path)`

Return the final path component, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `string|nil` - File name.

### `leviathan.fs.stem(path)`

Return the file stem, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `string|nil` - File stem.

### `leviathan.fs.extension(path)`

Return the final file extension, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `string|nil` - File extension.

### `leviathan.fs.join(a, b)`

Join two path strings.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `a` (`string`; required) - Base path.
- `b` (`string`; required) - Child path.

**Returns:**
- `string` - Joined path.

### `leviathan.fs.relative_to(path, base)`

Return path relative to base when path is under base.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path to rewrite.
- `base` (`string`; required) - Base directory.

**Returns:**
- `string|nil` - Relative path or nil.

### `leviathan.fs.with_extension(path, ext)`

Return path with its final extension replaced.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path to rewrite.
- `ext` (`string`; required) - New extension.

**Returns:**
- `string|nil` - Rewritten path or nil.

### `leviathan.fs.with_file_name(path, name)`

Return path with its final component replaced.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path to rewrite.
- `name` (`string`; required) - Replacement file name.

**Returns:**
- `string|nil` - Rewritten path or nil.

### `leviathan.fs.cwd()`

Return the process current working directory.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `string|nil` - Value on success, nil on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.home()`

Return the user home directory, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `string|nil` - Home directory.

### `leviathan.fs.temp_dir()`

Return the process temporary directory.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `string` - Temporary directory.

### `leviathan.fs.config_dir()`

Return the standard user config directory, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `string|nil` - Config directory.

### `leviathan.fs.cache_dir()`

Return the standard user cache directory, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `string|nil` - Cache directory.

### `leviathan.fs.data_dir()`

Return the standard user data directory, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `string|nil` - Data directory.

### `leviathan.fs.state_dir()`

Return the standard user state directory, or nil.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `string|nil` - State directory.

### `leviathan.fs.canonicalize(path)`

Resolve a path to an absolute canonical path.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path string.

**Returns:**
- `string|nil` - Value on success, nil on failure.
- `string|nil` - Error message on failure.

### `leviathan.fs.watch(path, opts, callback)`

Watch a path for filesystem changes and dispatch events to a callback.

**Capabilities:** `fs:watch`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `path` (`string`; required) - Path to watch (file or directory).
- `opts` (`LeviathanFsWatchOpts|nil`; optional) - { recursive = bool }.
- `callback` (`fun(event: LeviathanFsWatchEvent)`; required) - Callback invoked on each event.

**Returns:**
- `LeviathanFsWatchHandle` - Handle with `:cancel()` method.

# leviathan.git

Auto-generated by `cargo xtask gen-docs`. Do not edit by hand.

Typed Git write APIs. All routes are capability-checked, audited, and gated by the host's confirmation policy when destructive.

- Version: `1.0` (v1)
- Capabilities: `git:write:checkout`, `git:write:branch`, `git:write:tag`, `git:write:commit`, `git:write:stash`, `git:write:reset`, `git:write:fetch`, `git:write:push`, `git:write:merge`, `git:write:rebase`

## Events

- `HeadChanged` (table) - Fired after the resolved HEAD commit hash changes.
  - `hash` (`string`; required) - New HEAD commit hash.
- `BranchChanged` (table) - Fired after the current branch or its head hash changes.
  - `name` (`string`; required) - Branch name.
  - `head_hash` (`string`; required) - Resolved HEAD commit hash.
- `RefsChanged` (table) - Fired after the repository ref set changes (push/fetch/branch ops).
  - `count` (`integer`; required) - Number of refs after the change.
- `FetchStarted` (table) - Fired when a repository fetch starts.
  - `remote` (`string`; required) - Remote name involved in the fetch.
- `FetchFinished` (table) - Fired when a repository fetch finishes (success or failure).
  - `remote` (`string`; required) - Remote name involved in the fetch.
- `PushStarted` (table) - Fired when a push starts.
  - `remote` (`string`; required) - Remote name involved in the push.
- `PushFinished` (table) - Fired when a push finishes (success or failure).
  - `remote` (`string`; required) - Remote name involved in the push.

## Functions

### `leviathan.git.checkout(opts)`

Check out `ref`. Returns (true, nil) on enqueue + success, (false, err) otherwise.

**Capabilities:** `git:write:checkout`

**Parameters:**
- `opts` (`table`; required) - { ref = "main" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.create_branch(opts)`

Create branch `name` at `start_point` (defaults to HEAD).

**Capabilities:** `git:write:branch`

**Parameters:**
- `opts` (`table`; required) - { name = "topic", start_point = "HEAD" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.delete_branch(opts)`

Delete branch `name`. `force = true` requires destructive confirmation.

**Capabilities:** `git:write:branch`

**Parameters:**
- `opts` (`table`; required) - { name = "topic", force = false }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.create_tag(opts)`

Create lightweight tag `name` at `target` (defaults to HEAD).

**Capabilities:** `git:write:tag`

**Parameters:**
- `opts` (`table`; required) - { name = "v1.2.3", target = "HEAD" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.delete_tag(opts)`

Delete tag `name` locally. Destructive — requires confirmation.

**Capabilities:** `git:write:tag`

**Parameters:**
- `opts` (`table`; required) - { name = "v1.2.3" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.commit(opts)`

Commit currently-staged changes with `message`.

**Capabilities:** `git:write:commit`

**Parameters:**
- `opts` (`table`; required) - { message = "..." }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.stash_push(opts)`

Stash current dirty changes.

**Capabilities:** `git:write:stash`

**Parameters:**
- `opts` (`table`; optional) - { message = "from plugin" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.stash_pop(opts)`

Pop the most recent stash entry (or the entry at `index`).

**Capabilities:** `git:write:stash`

**Parameters:**
- `opts` (`table`; optional) - { index = 0 }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.reset(opts)`

Reset current branch to `ref` with `mode` (soft|mixed|hard). `hard` requires destructive confirmation.

**Capabilities:** `git:write:reset`

**Parameters:**
- `opts` (`table`; required) - { ref = "HEAD~1", mode = "soft|mixed|hard" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.fetch(opts)`

Fetch from `remote` (or every remote when omitted).

**Capabilities:** `git:write:fetch`

**Parameters:**
- `opts` (`table`; optional) - { remote = "origin" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.push(opts)`

Push the current branch to `remote`. Force pushes are not yet supported.

**Capabilities:** `git:write:push`

**Parameters:**
- `opts` (`table`; optional) - { remote = "origin", ref = "main" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.merge(opts)`

Merge `ref` into the current branch. Destructive — requires confirmation.

**Capabilities:** `git:write:merge`

**Parameters:**
- `opts` (`table`; required) - { ref = "topic" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.

### `leviathan.git.rebase(opts)`

Rebase the current branch onto `ref`. Destructive — requires confirmation.

**Capabilities:** `git:write:rebase`

**Parameters:**
- `opts` (`table`; required) - { ref = "main" }

**Returns:**
- `boolean` - True on success, false on failure.
- `string|nil` - Error message on failure.


# leviathan.ui.dock

Auto-generated by `cargo xtask gen-docs`. Do not edit by hand.

Persistent dock panel namespace.

- Version: `1.0` (v1)
- Stability: `stable`
- Migration promise: source-compatible additions only within host 1.x
- Capabilities: `ui:dock`

## Types

### `leviathan.ui.dock`

Persistent dock panel namespace.

### `LeviathanDockPanelSpec`

Persistent dock panel registration spec.

- `id` (`string`; required) - Plugin-local panel id.
- `title` (`string`; required) - Panel title shown by host chrome.
- `area` (`string`; required) - Dock area: left, right, bottom, graph, diff, tab, or floating.
- `default_open` (`boolean`; optional) - Initial open state when no user layout exists.
- `view` (`fun(ctx: DockPanelContext): LeviathanWidget`; required) - Render callback.
- `update` (`fun(state: table, event: string, value: LeviathanJson): table|nil`; optional) - Event callback. Return `{ state = next_state }` to persist panel state.

### `LeviathanDockPanelHandle`

Dock panel registration handle.

- `plugin_id` (`string`; required) - Owner plugin id.
- `id` (`string`; required) - Plugin-local panel id.
- `key` (`string`; required) - Stable host panel key.
- `title` (`string`; required) - Panel title.
- `area` (`string`; required) - Current dock area.

### `DockPanelContext`

Context passed to dock panel render callbacks.

- `version` (`integer`; required) - Context schema version.
- `plugin_id` (`string`; required) - Current plugin id.
- `generation_id` (`integer`; required) - Current plugin generation.
- `type` (`string`; required) - Concrete context type name.
- `surface` (`string`; required) - Current coarse UI surface.
- `features` (`table<string, boolean>`; required) - Relevant feature gates.
- `theme` (`LeviathanUiThemeTokens`; required) - Stable host theme tokens.
- `repository` (`LeviathanRepositorySummary`; required) - Active repository summary without commit lists or diffs.
- `tab` (`LeviathanTabSummary`; required) - Active tab summary.
- `selection` (`LeviathanSelectionSummary`; required) - Selection summary when the host has one for the surface.
- `focus` (`LeviathanFocusSummary`; required) - Focused surface, region, pane, and section where available.
- `viewport` (`LeviathanViewportSummary`; required) - Viewport constraints when known.
- `payload` (`table`; required) - Surface-specific ids and lightweight metadata.

## Functions

### `leviathan.ui.dock.register(spec)`

Register a persistent dock panel with host-owned layout state.

**Capabilities:** `ui:dock`

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `spec` (`LeviathanDockPanelSpec`; required) - Dock panel descriptor.

**Returns:**
- `LeviathanDockPanelHandle|nil` - Dock panel handle on success.
- `string|nil` - Error message on failure.

# leviathan.keymap

Auto-generated by `cargo xtask gen-docs`. Do not edit by hand.

Context-aware keymap registry.

- Version: `1.0` (v1)
- Stability: `stable`
- Migration promise: source-compatible additions only within host 1.x

## Types

### `leviathan.keymap`

Context-aware keymap registry namespace.

### `LeviathanKeymapOpts`

Options table accepted by `leviathan.keymap.set`.

- `description` (`string`; optional) - Description shown in devtools and the keymap inspector.
- `args` (`LeviathanJson`; optional) - Args table forwarded verbatim to the underlying command at dispatch time.

### `LeviathanKeymapSummary`

Compact view of a registered keymap, returned by `leviathan.keymap.list`.

- `context` (`string`; required) - Activation context.
- `key` (`string`; required) - Rendered chord (vim-style).
- `command` (`string`; required) - Command the keymap dispatches to.
- `plugin_id` (`string`; required) - Owning plugin id; `<host>` for built-ins, `<user>` for user-config rows.
- `source` (`string`; required) - One of `built-in`, `user`, `plugin`.
- `status` (`string`; required) - One of `active`, `conflict_lost`.
- `description` (`string`; required) - Description supplied at registration.
- `conflict_with` (`LeviathanKeymapConflictRef|nil`; optional) - When `status == conflict_lost`, points to the winning binding.

### `LeviathanKeymapConflictRef`

Reference to the winning binding for a conflict-lost keymap row.

- `plugin_id` (`string`; required) - Winning binding's plugin id.
- `source` (`string`; required) - Winning binding's source tier.

## Events

- `KeymapTriggered` (table) - Fired after a keymap chord matches and the host dispatches the underlying command.
  - `context` (`string`; required) - Active context the chord matched in.
  - `key` (`string`; required) - Rendered chord (vim-style notation).
  - `command` (`string`; required) - Command the chord dispatched to.
  - `plugin_id` (`string`; required) - Owning plugin id of the matched keymap; `<host>` for built-ins, `<user>` for user-config.
  - `ok` (`boolean`; required) - True when the underlying command dispatch returned `Ok`.
- `KeymapPrefixChanged` (table) - Fired when the in-flight keymap prefix changes so plugins can render their own hint UI.
  - `active` (`boolean`; required) - True while the host is buffering a keymap prefix.
  - `context` (`string`; required) - Active keymap context.
  - `prefix` (`string`; required) - Rendered in-flight chord prefix.
  - `hints` (`table`; required) - Array of next-key hint rows after conflict filtering.
  - `reason` (`string`; required) - Why the prefix state changed (`pending`, `dispatched`, `cancelled`, `unhandled`, `context_changed`, or `focus_lost`).

## Functions

### `leviathan.keymap.set_leader(leader)`

Set the active keymap leader sequence used to expand `<leader>` bindings.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `leader` (`string`; required) - Vim-style key sequence used as leader, e.g. `<Space>` or `,`.

### `leviathan.keymap.set(context, key, command, opts)`

Bind a key chord to a registered command in a context.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `context` (`string`; required) - Activation context (`global`, `repository`, `repository.graph`, `tab_bar`, etc.).
- `key` (`string`; required) - Vim-style key sequence (e.g. `gl`, `<C-r>`, `<leader>gh`).
- `command` (`string`; required) - Command name resolved by the host command registry at dispatch time.
- `opts` (`LeviathanKeymapOpts`; optional) - Keymap options (description, args).

### `leviathan.keymap.del(context, key)`

Remove a previously-registered keymap owned by the calling plugin generation.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Parameters:**
- `context` (`string`; required) - Activation context the keymap was bound under.
- `key` (`string`; required) - Vim-style key sequence.

### `leviathan.keymap.list()`

List every registered keymap (built-in, user, plugin) including conflict losers.

**Since:** `1.0`
**Stability:** `stable` (v1)

**Returns:**
- `LeviathanKeymapSummary[]` - Array of keymap summaries with conflict status.
